[package]
name = "rdpcore-dvc"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "From-scratch MS-RDPEDYC (drdynvc) dynamic virtual channel multiplexer"

[features]
default = []
echo = []

[dependencies]
rdpcore-pdu = { path = "../rdpcore-pdu" }
